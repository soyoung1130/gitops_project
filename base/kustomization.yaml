apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 여기에 정의된 파일들을 하나로 묶습니다.
resources:
  - deployment.yaml
  - service.yaml
  - route.yaml

# 공통으로 적용될 이미지 이름 (태그는 비워둡니다)
images:
  - name: registry.ocp.test.com:5000/demo/test-project